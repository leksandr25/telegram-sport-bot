# telegram-sport-bot (Vercel webhook example)

Це приклад Telegram webhook-бота на Python (aiogram 3), який підходить для розгортання на Vercel.

## Функціонал
- Адмін групи викликає `/event` у групі.
- Бот покроково запитує: назву, дату/час, опис (необов'язково).
- Після підтвердження бот публікує повідомлення в групі з кнопками **👍 Буду** та **👎 Не буду**.
- Бот веде підрахунок голосів і оновлює повідомлення (числа в кнопках).

## Як використовувати
1. Форкни репозиторій або завантаж цей проект.
2. Додай в Vercel як новий проект (Import Git repo).
3. У Vercel → Settings → Environment Variables додай:
   - `BOT_TOKEN` = твій Telegram Bot API token
4. Деплой. Після деплою візьми URL і встанови webhook:
   ```
   https://api.telegram.org/bot<YOUR_TOKEN>/setWebhook?url=https://<your-vercel-url>/api/webhook
   ```
5. Використовуй команду `/event` в групі (тільки адміністратори можуть створювати події).

## Важливо
- Це серверлес-реалізація: дані зберігаються в оперативній пам'яті процесу. При холодному старті / повторному запуску дані втратяться.
- Для збереження подій довше використай персистентну базу (SQLite/Postgres) або KV.
